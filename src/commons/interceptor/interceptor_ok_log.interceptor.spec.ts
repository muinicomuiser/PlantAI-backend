import { InterceptorOkLogInterceptor } from '../interceptor_ok_log/interceptor_ok_log.interceptor';

describe('InterceptorOkLogInterceptor', () => {
  it('should be defined', () => {
    expect(new InterceptorOkLogInterceptor()).toBeDefined();
  });
});
